/******************************************************************************* 
 * @brief   This is a multi-line comment.
 *
 * Obviously, it needs to span multiple lines.
 * As of now, comments are not nestable, so I can include /* /* /* /* /* /*...
 * without affecting the correctness.
 * Here is an absolutely empty line:

 * Ending with multiple '*' before the final slash will also stress test
 * my compiler to make sure that it stays on its toes, ready to end the
 * comment at any time!
 ******************************************************************************/