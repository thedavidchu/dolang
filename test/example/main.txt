let io, vector = import("stdio"), import("stdvector");

let print: function = io.out;
let println: function = function (txt, *args, **kwargs) { print("{txt}\n", *args, **kwargs); }
let vector_u8: type = vector[int[0..255]];

/// Documentation goes here!
// This is a regular comment
/* This is a block comment. */
" This is ignored, but still in the program. "
""" Same with this, but it's multi-line."""
# instructions for various things can go here
function sum(x: const vector_u8, y: const vector_u8) -> u8: pure compile {
    let z: i8 = x + y;
    /* Not checking for wrap around. */
    return z;
}


function main() -> () {
    print("Hello, World!\n");
    print("Sum of 255 + 255 = {sum(x=255, y=255)\n");
    return 0;
}